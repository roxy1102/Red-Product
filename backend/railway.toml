[build]
builder = "nixpacks"

[env]
APP_ENV = "production"
APP_KEY = "base64:xxxxx"   # tu peux laisser vide, Railway utilisera .env
APP_DEBUG = "false"

[start]
# Ici on utilise php artisan serve avec la vraie variable d'environnement
cmd = "php artisan serve --host=0.0.0.0 --port=${PORT}"
